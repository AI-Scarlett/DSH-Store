# ADR-0001：首版只读

状态：已接受。

## 决策

首版只展示本地插件清单，不提供安装、卸载、启停、更新、修复或后台自动任务。

## 原因

插件管理的写路径会影响 Profile manifest、Bundle 层、lockfile 和 `node_modules`。
在来源识别、运行态映射与真实 DSH 集成尚未稳定前引入写入，会放大启动失败和配置
覆盖风险。只读首版可以先验证扩展机制、信息模型与 UI，而不改变运行环境。

## 后果

用户暂时只能查看；任何未来写入都必须作为新里程碑，经独立安全评审和验收。

